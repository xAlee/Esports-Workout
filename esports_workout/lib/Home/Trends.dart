import 'package:flutter/material.dart';

class Trends extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white, // Color de fondo de la página Home
      child: Center(
        child: Text('Trends'),
      ),
    );
  }
}
